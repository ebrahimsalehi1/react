import {createStyles} from '@material-ui/core/styles'

export const useStyles = createStyles(theme=>({
    button: {
        margin: '0 5px',
        fontSize: 11,
    },     
    input: {
      display: 'none',
    },         
  }))

